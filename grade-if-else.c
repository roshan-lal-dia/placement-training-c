#include <stdio.h>

int main() {
    char grade = "";
    int marks = 0;

    if (marks <=100 || >=90){
        grade = "S";
        printf("Grade : %c", grade);
    }else if (marks <=90 || >=80)
    {
        grade = "S";
        printf("Grade : %c", grade);

    }else if (marks <=90 || >=80)
    {
        grade = "S";
        printf("Grade : %c", grade);

    }
    
}